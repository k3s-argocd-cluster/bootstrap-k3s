### Generated by ansible :: demelopment.k3s.argocd@render_templates ###
apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-repo-server
spec:
  template:
    spec:
      containers:
      - name: kustomized-helm-plugin-cmp
        command: [/var/run/argocd/argocd-cmp-server] # Entrypoint should be Argo CD lightweight CMP server i.e. argocd-cmp-server
        image: quay.io/argoproj/argocd:v2.9.1
        securityContext:
          runAsNonRoot: true
          runAsUser: 999
        volumeMounts:
          - mountPath: /var/run/argocd
            name: var-files
          - mountPath: /home/argocd/cmp-server/plugins
            name: plugins
          - mountPath: /home/argocd/cmp-server/config/plugin.yaml # Plugin config file can either be volume mapped or baked into image
            subPath: plugin.yaml
            name: kustomize-helm-cmp-plugin
          - mountPath: /tmp
            name: kustomized-helm-plugin-cmp-tmp
      volumes:
        - configMap:
            name: kustomize-helm-cmp-plugin
          name: kustomize-helm-cmp-plugin
        - emptyDir: {}
          name: kustomized-helm-plugin-cmp-tmp